[package]
name = "drumbox64-asid"
version = "0.1.0"
edition = "2021"

[features]
default = []
hardware = ["dep:usbsid-pico"]

[dependencies]
drumbox64-core = { path = "../core" }
usbsid-pico = { version = "0.1.9", features = ["usb"], optional = true }
